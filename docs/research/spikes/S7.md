# S7: categories created through Graph, applied to a task

Run: 2026-09-24 14:18:51–14:19:24 UTC. Throwaway list `{L}`. Auth header redacted. The account's 7 pre-existing categories were
only counted, never modified.

## Graph-side answer

- `POST /me/outlook/masterCategories` `{"displayName":"ms-todo-spike My Day","color":"preset4"}` → **201**, id `{C1}`
  (request-id 40e207f4-2ced-45f7-82e7-5f3827f58e6b). The `Location` header exposes an internal `outlook_…@outlook.com` user id (redacted here).
- `POST /me/todo/lists/{L}/tasks` `{"title":"S7 My Day category task (check on phone)","categories":["ms-todo-spike My Day"]}` → 201, `categories` round-trips.
- `PATCH` an existing task `{"categories":["ms-todo-spike My Day"]}` → 200; `categories` is a whole-array replace.
- Next `tasks/delta` returned all three tasks with their `categories` intact.
- `GET …/tasks?$filter=categories/any(c:c eq 'ms-todo-spike My Day')` → 200, returns exactly the two tagged tasks. (Server-side category filter works.)
- **Colour correction:** Microsoft's outlookCategory doc maps `preset4` = **Green**, `preset3` = **Yellow** (Outlook desktop mapping;
  "the actual color is dependent on the Outlook client"). Blueprint 05 says preset4 is "yellowish": that is wrong per the doc.
  A second category `ms-todo-spike My Day yellow` (`preset3`, id `{C2}`) was created and added to the same task, so BK can compare both on the phone.
- **Name with no master category:** `POST …/tasks` `{"title":"S7 ghost category task","categories":["ms-todo-spike ghost-category"]}` → 201, the string is stored and
  returned by GET and delta. **No master category is auto-created** (count went 7→8 only from our own POST, then 9 with the yellow one).
- **Master category names are immutable and unique case-insensitively:**
  - `PATCH /me/outlook/masterCategories/{id}` `{"displayName":"ms-todo-spike renamed"}` → **200 but ignored** (name unchanged on GET).
  - `POST` `"ms-todo-spike My Day"` again → **409** `CategoryNameExists` "Category name already exists."
  - `POST` `"MS-TODO-SPIKE my day"` → **409** `CategoryNameExists` (case-insensitive).

Orchestrator follow-up (2026-09-24): colour can be changed. `PATCH /me/outlook/masterCategories/{id}` `{"color":"preset7"}` on a temporary category returned 200, and a GET showed `preset7`.

Confidence (Graph side): high.

## Phone result (BK, iPhone, 2026-09-24)

The iOS To Do app shows **no categories at all**: not on the list row, not in the task detail view. So there's no colour to judge and no way to filter. The Graph side above still holds. Consequence: D-037 (My Day lives in our extension, and reaches the phone through the due date instead).

## BK's phone check (done 2026-09-24; the steps as given)

1. Open Microsoft To Do on the phone, open the list **ms-todo-spike-2026-09-24**.
2. Find **"S7 My Day category task (check on phone)"**. Does the list row show category tags? Note the two tag colours: "ms-todo-spike My Day" (preset4, doc says green) and "ms-todo-spike My Day yellow" (preset3, doc says yellow). Which looks right for My Day?
3. Open the task. Are both categories shown in the detail view? Can you add or remove a category there?
4. Find **"S7 ghost category task"**: is "ms-todo-spike ghost-category" shown at all, and in what colour (none/grey)?
5. Filtering: in the search box, type `ms-todo-spike My Day`. Do tagged tasks come up? Also check whether there is any category filter/"Tagged" smart list or a way to tap a category tag to see all tasks with it.
6. Also glance at "S2 extension task" (tagged via PATCH on an existing task): does its tag show too?

Record: shows on list row (y/n), shows in detail (y/n), colour of each, ghost shown (y/n), can filter/search by category (how).

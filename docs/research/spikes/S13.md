# S13: can a create carry a unique marker for outcome reconciliation? (orchestrator, 2026-09-24T14:50:59Z)

Added after the independent review (gpt-6-sol) showed field matching can't attribute an uncertain create.

1. `POST /me/todo/lists/{throwaway}/tasks` body `{"title":"S13 inline extension on create","extensions":[{"@odata.type":"microsoft.graph.openTypeExtension","extensionName":"com.planetaryescape.mstodo","opId":"op-7f3a"}]}`
   → **201**. The response includes `extensions: [{"extensionName":"com.planetaryescape.mstodo","id":"microsoft.graph.openTypeExtension.com.planetaryescape.mstodo","opId":"op-7f3a"}]`.
2. `GET …/tasks/{id}?$expand=extensions($filter=id eq 'com.planetaryescape.mstodo')` → the extension comes back with `opId: "op-7f3a"`. The short name works in the filter even though the stored id is fully qualified.
3. `GET …/tasks?$filter=extensions/any(e:e/id eq 'com.planetaryescape.mstodo')` → **400 invalidRequest**. You can't filter on extensions server-side. Match `opId` client-side over a `lastModifiedDateTime ge …` collection GET with the filtered `$expand`.
4. Control: a plain POST without extensions → 201.

Conclusion (high confidence): a task create can atomically carry its outbox op ID in our extension, so a lookup can attribute the created task exactly. Checklist items and linked resources have no extensions, so this doesn't cover them.

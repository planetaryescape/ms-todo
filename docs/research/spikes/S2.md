# S2: do lists/delta and tasks/delta report open-extension changes? Does $expand=extensions work?

Run: 2026-09-24 14:13–14:15 UTC. Throwaway list `{L}` (see S1), task "S2 extension task". Extension name `com.planetaryescape.mstodo`.
Auth header redacted everywhere.

## Answer

- **Change detection: yes.** Creating, PATCHing or deleting an open extension bumps the parent's `@odata.etag` (and for tasks
  `lastModifiedDateTime`), and the parent appears in the next `tasks/delta` / `lists/delta` round. Confidence: high.
- **Content via delta: no.** Delta never returns extension data. `$expand=extensions` is accepted (200) but silently returns nothing;
  `$expand=extensions($filter=id eq '…')` on delta is a **400**. Confidence: high.
- **Content via plain GET / list: yes, but only with a filtered expand.** `$expand=extensions` alone returns nothing (silently);
  `$expand=extensions($filter=id eq 'com.planetaryescape.mstodo')` works on single GET and on collection GET, for lists and tasks.
  It also combines with `$filter=lastModifiedDateTime ge …` and `$orderby`/`$top`.

## Requests

| Request | Status | Result |
|---|---|---|
| `POST /me/todo/lists/{L}/tasks/{T}/extensions` `{"@odata.type":"microsoft.graph.openTypeExtension","extensionName":"com.planetaryescape.mstodo","myDay":"2026-09-24","assignee":"Sam"}` | 201 | body returned; task etag `…EHE0ag==` → `…EHE0pQ==`, lastModified 14:11:38.586 → 14:13:24.876 |
| next `tasks/delta` (plain and `$expand=checklistItems,extensions` cursors) | 200 | task present, **no `extensions` key** |
| `GET …/tasks/{T}?$expand=extensions` | 200 | no `extensions` key |
| `GET …/tasks/{T}?$expand=extensions($filter=id eq 'com.planetaryescape.mstodo')` | 200 | `extensions:[{"extensionName":"com.planetaryescape.mstodo","id":"microsoft.graph.openTypeExtension.com.planetaryescape.mstodo","myDay":"2026-09-24","assignee":"Sam"}]` |
| `GET …/tasks/{T}/extensions` (collection) | **404** `UnknownError`, empty message | |
| `GET …/tasks/{T}/extensions/com.planetaryescape.mstodo` | 200 | the extension |
| `GET …/tasks?$expand=extensions` | 200 | no extensions on any task |
| `GET …/tasks?$expand=extensions($filter=id eq '…')` | 200 | extension on the S2 task, `null` on others |
| `GET …/tasks?$filter=lastModifiedDateTime ge 2026-09-24T14:13:00Z&$expand=extensions($filter=id eq '…')` | 200 | works |
| `GET …/tasks/delta?$expand=extensions($filter=id eq '…')` | **400** `BadRequest` "Parsing OData Select and Expand failed: Value cannot be null. (Parameter 'initialState')" | |
| `GET …/tasks/delta?$expand=extensions($filter=extensionName eq '…')` | 400, same message | |
| `GET …/tasks/delta?$expand=extensions($select=id)` | 200 | no extensions |
| `PATCH …/tasks/{T}/extensions/com.planetaryescape.mstodo` `{"myDay":"2026-09-25"}` | 200 | **replaced the whole extension: `assignee` was dropped** (GET after: `{"myDay":"2026-09-25"}` only). Task appeared in delta with new etag `…EHE0qw==` |
| `POST …/tasks/{S1}/extensions` then `DELETE …/extensions/com.planetaryescape.mstodo` | 201, 204 | delete bumped the task (appeared in delta, etag `…EHE0xA==`) |
| `POST` the same extensionName again with `x:2` when it already exists | **201** | acts as upsert; GET shows one extension, `x:2` |
| `DELETE` an extension that doesn't exist | **204** | idempotent |

Lists:

| Request | Status | Result |
|---|---|---|
| `POST /me/todo/lists/{L}/extensions` `{"@odata.type":"microsoft.graph.openTypeExtension","extensionName":"com.planetaryescape.mstodo","folder":"Spike Folder","order":3}` | 201, **empty body** | list etag `…EHE0YQ==` → `…EHE0rw==` |
| next `lists/delta` | 200 | the list appears (1 item), no extension data |
| `PATCH /me/todo/lists/{L}/extensions/com.planetaryescape.mstodo` `{"folder":"Spike Folder 2"}` | 200 | again **replaces** (`order` gone); list appears in `lists/delta` (etag `…EHE0tg==`) |
| `GET /me/todo/lists/{L}?$expand=extensions($filter=id eq '…')` | 200 | `extensions:[{…,"folder":"Spike Folder","order@odata.type":"#Int64","order":3}]` |
| `GET /me/todo/lists?$expand=extensions($filter=id eq '…')` | 200 | 30 lists, extension on the throwaway list only |
| `GET /me/todo/lists?$expand=extensions` | 200 | no extensions anywhere |
| `GET /me/todo/lists/delta?$expand=extensions` | 200 | no extensions |
| `GET /me/todo/lists/delta?$expand=extensions($filter=id eq '…')` | **400** same "initialState" error | |

Integers come back with an `@odata.type: "#Int64"` annotation (`order@odata.type`), so extension deserialisation must ignore `*@odata.type` keys.

## Conclusion

Extension changes are *detected* by delta (etag/lastModified bump), but their *content* has to be fetched separately. Two cheap ways:

- Lists: after every `lists/delta` round that reports any list, one `GET /me/todo/lists?$expand=extensions($filter=id eq 'com.planetaryescape.mstodo')` (one request for all ~30 lists).
- Tasks: for tasks reported by delta, `GET /me/todo/lists/{l}/tasks?$filter=lastModifiedDateTime ge {since}&$expand=extensions($filter=id eq '…')`
  (one request per changed list), or `$batch` of per-task GETs with the same filtered expand.

Writes: extension PATCH is a full replace, so always send the whole document (or POST it, which upserts). Confidence: high.
